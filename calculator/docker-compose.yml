version: "3.8"
services:
  calculator:
    build:
      context: .
      dockerfile: calculator/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    restart: unless-stopped